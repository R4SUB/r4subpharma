% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{as_variable_metadata}
\alias{as_variable_metadata}
\title{Coerce Metadata to the Variable-Metadata Contract}
\usage{
as_variable_metadata(metadata)
}
\arguments{
\item{metadata}{Either a \code{Metacore} object or a data.frame with at least the
columns \code{dataset} and \code{variable}. Optional columns \code{label}, \code{type},
\code{origin}, \code{derivation}, and \code{is_derived} are used when present.}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{dataset}, \code{variable},
\code{label}, \code{type}, \code{origin}, \code{derivation}, and \code{is_derived}.
}
\description{
Normalizes the accepted metadata inputs into a single tidy table with one row
per dataset variable. A \code{metacore} object is unpacked
into its dataset, variable, and derivation components; a data.frame is checked
for the required \code{dataset} and \code{variable} columns and completed with any
missing optional columns.
}
\examples{
\dontshow{if (requireNamespace("r4subdata", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# The example ADaM metadata shipped in r4subdata.
meta <- as_variable_metadata(r4subdata::adam_metadata)
head(meta)
\dontshow{\}) # examplesIf}
}
