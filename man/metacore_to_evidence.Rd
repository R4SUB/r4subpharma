% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metacore_to_evidence.R
\name{metacore_to_evidence}
\alias{metacore_to_evidence}
\title{Convert Metadata to Submission Readiness Evidence}
\usage{
metacore_to_evidence(
  metadata,
  ctx,
  source_name = "metacore",
  source_version = NULL
)
}
\arguments{
\item{metadata}{A \code{Metacore} object or a data.frame accepted by
\code{\link[=as_variable_metadata]{as_variable_metadata()}}.}

\item{ctx}{An \link[r4subcore:r4sub_run_context]{r4subcore::r4sub_run_context} providing run and study
identifiers.}

\item{source_name}{Character. Label recorded as the evidence source.
Default \code{"metacore"}.}

\item{source_version}{Character or \code{NULL}. Optional version label for the
metadata source.}
}
\value{
A data.frame conforming to the R4SUB evidence schema.
}
\description{
Turns dataset metadata - either a \code{metacore} object or a
plain data.frame - into standardized R4SUB evidence rows describing how
completely each variable is documented. This lets metadata assembled for a
'pharmaverse' pipeline feed the same evidence table and Submission Confidence
Index as any other source.
}
\details{
Two indicators are evaluated, reusing the identifiers emitted by
\code{r4subcore::define_xml_to_evidence()} so that metadata- and Define-XML-sourced
evidence group together in scoring and explainability:
\describe{
\item{Q-DEFINE-002}{Variable is documented (has a label and a data type).}
\item{Q-DEFINE-003}{Derivation text is present for derived variables.}
}
}
\examples{
\dontshow{if (requireNamespace("r4subdata", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# The example ADaM metadata shipped in r4subdata.
ctx <- suppressMessages(r4subcore::r4sub_run_context("STUDY01", "DEV"))
ev  <- suppressMessages(metacore_to_evidence(r4subdata::adam_metadata, ctx))
table(ev$indicator_id, ev$result)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=adam_to_evidence]{adam_to_evidence()}}, \code{\link[=submission_readiness]{submission_readiness()}}
}
