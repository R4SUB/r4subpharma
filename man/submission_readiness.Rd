% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/submission_readiness.R
\name{submission_readiness}
\alias{submission_readiness}
\title{Score Submission Readiness for a 'pharmaverse' Deliverable}
\usage{
submission_readiness(data, metadata, ctx, config = NULL)
}
\arguments{
\item{data}{A named list of ADaM data.frames (names are dataset identifiers,
e.g. \code{"ADSL"}), or a single data.frame when the metadata covers one
dataset.}

\item{metadata}{A \code{Metacore} object or a data.frame accepted by
\code{\link[=as_variable_metadata]{as_variable_metadata()}}.}

\item{ctx}{An \link[r4subcore:r4sub_run_context]{r4subcore::r4sub_run_context} providing run and study
identifiers.}

\item{config}{An optional \code{sci_config} (see
\code{\link[r4subscore:sci_config_default]{r4subscore::sci_config_default()}}). When \code{NULL}, the r4subscore default is
used.}
}
\value{
An object of class \code{"submission_readiness"}: a list with \code{evidence}
(the combined evidence data.frame), \code{pillar_scores}, and \code{sci} (an
\code{sci_result}, or \code{NULL} if 'r4subscore' is not installed).
}
\description{
A single entry point that harvests evidence from metadata and one or more
ADaM datasets and, when 'r4subscore' is available, computes the Submission
Confidence Index (SCI) in one call. It is the fastest way to go from a
'pharmaverse' pipeline to a readiness score.
}
\examples{
meta <- data.frame(
  dataset  = "ADSL",
  variable = c("USUBJID", "AGE"),
  label    = c("Unique Subject Identifier", "Age"),
  type     = c("text", "integer"),
  stringsAsFactors = FALSE
)
adsl <- data.frame(USUBJID = "01-001", AGE = 54, stringsAsFactors = FALSE)
ctx <- suppressMessages(r4subcore::r4sub_run_context("STUDY01", "DEV"))
res <- suppressMessages(submission_readiness(list(ADSL = adsl), meta, ctx))
nrow(res$evidence)

}
\seealso{
\code{\link[=metacore_to_evidence]{metacore_to_evidence()}}, \code{\link[=adam_to_evidence]{adam_to_evidence()}}
}
