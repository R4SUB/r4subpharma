% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adam_to_evidence.R
\name{adam_to_evidence}
\alias{adam_to_evidence}
\title{Convert an ADaM Dataset to Submission Readiness Evidence}
\usage{
adam_to_evidence(
  data,
  metadata,
  ctx,
  dataset_name = NULL,
  source_name = "adam",
  source_version = NULL
)
}
\arguments{
\item{data}{A data.frame: one ADaM dataset.}

\item{metadata}{A \code{Metacore} object or a data.frame accepted by
\code{\link[=as_variable_metadata]{as_variable_metadata()}}.}

\item{ctx}{An \link[r4subcore:r4sub_run_context]{r4subcore::r4sub_run_context} providing run and study
identifiers.}

\item{dataset_name}{Character or \code{NULL}. Which dataset in \code{metadata} to check
\code{data} against. May be omitted when the metadata covers a single dataset.}

\item{source_name}{Character. Label recorded as the evidence source.
Default \code{"adam"}.}

\item{source_version}{Character or \code{NULL}. Optional version label.}
}
\value{
A data.frame conforming to the R4SUB evidence schema.
}
\description{
Compares a built ADaM dataset - such as one produced by an 'admiral' pipeline
\itemize{
\item against its metadata and emits R4SUB evidence rows about conformance. The
checks are computed directly from the dataset and metadata, so they work
whether or not the dataset was passed through 'xportr' or 'metatools'.
}
}
\details{
Four indicators are evaluated:
\describe{
\item{T-ADAM-001}{Each variable described in the metadata is present in the
dataset (traceability).}
\item{T-ADAM-002}{Each dataset column is described in the metadata; columns
that are not are flagged (traceability).}
\item{Q-ADAM-001}{Present variables have the data type family the metadata
specifies (quality).}
\item{Q-ADAM-002}{Present variables carry a non-empty label attribute
(usability).}
}
}
\examples{
meta <- data.frame(
  dataset  = "ADSL",
  variable = c("USUBJID", "AGE", "SEX"),
  label    = c("Unique Subject Identifier", "Age", "Sex"),
  type     = c("text", "integer", "text"),
  stringsAsFactors = FALSE
)
adsl <- data.frame(
  USUBJID = c("01-001", "01-002"),
  AGE     = c(54, 61),
  stringsAsFactors = FALSE
)
ctx <- suppressMessages(r4subcore::r4sub_run_context("STUDY01", "DEV"))
ev <- suppressMessages(adam_to_evidence(adsl, meta, ctx, dataset_name = "ADSL"))
nrow(ev)

}
\seealso{
\code{\link[=metacore_to_evidence]{metacore_to_evidence()}}, \code{\link[=submission_readiness]{submission_readiness()}}
}
